# Fichier d'exemple pour la configuration de l'environnement
# Copiez ce fichier en .env et configurez les valeurs appropriées

# Clé API Mistral pour l'analyse personnalisée
# Obtenez votre clé API sur https://mistral.ai
MISTRAL_API_KEY="votre_cle_api_mistral_ici"

# Configuration de la base de données (optionnelle)
# Par défaut, SQLite est utilisé avec le fichier transformation.db
# DATABASE_URL="sqlite:///./transformation.db"

# Configuration du serveur (optionnelle)
# HOST="0.0.0.0"
# PORT="8000"

# Configuration CORS (optionnelle)
# CORS_ALLOW_ORIGINS="https://localhost:5173,http://localhost:5173"

# Environnement (development ou production)
# En production : désactive /docs et /redoc, active HSTS
# ENVIRONMENT="development"

# Authentification JWT
# Clé secrète pour signer les tokens (générée automatiquement si absente, mais non persistante)
# JWT_SECRET_KEY="votre_cle_secrete_longue_et_aleatoire"
# Durée de validité des tokens en minutes (défaut : 1440 = 24h)
# JWT_EXPIRE_MINUTES="1440"

# SSL/TLS
# Les certificats sont lus automatiquement depuis le dossier certs/ à la racine du projet.
# Pour générer des certificats auto-signés : bash generate-certs.sh
